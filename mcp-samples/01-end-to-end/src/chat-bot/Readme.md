# How to test Chat Bot

NOTE:

- tested on windows with node.js based mcp server
- ensure `rest-api-node-js` is running at http://localhost:3000

### Steps

- create `.env` from `.env.dev` (or just rename!)
- run following command

```
npm install
npm start
```
