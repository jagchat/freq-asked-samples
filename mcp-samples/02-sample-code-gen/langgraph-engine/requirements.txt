# Web Framework
fastapi>=0.115.5          # Modern web framework for building APIs
uvicorn[standard]>=0.32.1 # ASGI server (web server) to run FastAPI

# LangGraph and LangChain
langgraph>=0.2.53         # Workflow orchestration with state graphs
langchain>=0.3.13         # LLM application framework
langchain-core>=0.3.23    # Core LangChain abstractions
langchain-community>=0.3.13 # Community integrations (includes ChatLiteLLM)

# LLM Providers
langchain-openai>=0.2.14  # OpenAI integration (GPT-4, GPT-3.5)
langchain-anthropic>=0.3.3 # Anthropic integration (Claude)
langchain-ollama>=0.2.2   # Ollama integration (local models)
litellm>=1.55.11          # LiteLLM for unified access to 100+ LLM providers

# HTTP Client
httpx>=0.28.1             # Async HTTP client (used by LangChain)

# Environment Variables
python-dotenv>=1.0.1      # Load environment variables from .env file

# Utilities
pydantic>=2.10.4          # Data validation using Python type hints
pydantic-settings>=2.7.0  # Settings management with Pydantic
